%article.pos-rel
  - notify_posts_count = redis_scard_posts("poll:#{poll.id}:posts")
  - if notify_posts_count > 0
    %span.badge.badge-post= notify_posts_count
  .main.pl20
    %header.no-bs
      %h3= link_to poll.title, poll
