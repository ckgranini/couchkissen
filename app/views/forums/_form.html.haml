=form_for @forum do |f|
  - if @forum.errors.any?
    %ul
      - @forum.errors.full_messages.each do |msg|
        %li= msg
  .form
    = f.label :title
    = f.text_field :title
    .ta-right
      = f.submit :class => "button"