%article
  .main.pl20
    %header.no-bs
      %small
        - if authorized?(@forum.user)
          = link_to t("links.edit"), edit_forum_path(@forum)
          %span= "|"
          = link_to t("links.delete"), @forum, method: :delete, confirm: t("messages.confirm_delete")
      %h3= @forum.title

= render "posts/new"
= render @posts
%article
  = will_paginate @posts, :class => 'main ta-center', :inner_window => 3